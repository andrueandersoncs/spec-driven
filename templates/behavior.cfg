\* TLC Configuration File
\* Generated by spec-driven plugin
\*
\* This file configures the TLC model checker

\* The specification to check
SPECIFICATION Spec

\* Type invariant (checked at every state)
INVARIANT TypeOK

\* Safety properties (checked at every state)
\* INVARIANT BalanceNonNegative
\* INVARIANT NoDoubleProcessing

\* Liveness properties (temporal)
\* PROPERTY RequestCompletion

\* Constants (define values for model checking)
\* CONSTANT
\*     MaxBalance = 1000
\*     MaxTransactions = 10

\* State constraint (limit state space for tractable checking)
\* CONSTRAINT
\*     balance <= MaxBalance
\*     Len(transactions) <= MaxTransactions

\* Symmetry (reduce state space if variables are interchangeable)
\* SYMMETRY Symmetry

\* Check for deadlock (no enabled actions)
CHECK_DEADLOCK TRUE
